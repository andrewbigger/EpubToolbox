options:
  docker: true
 
image:
  name: josediaz30/oracle-jdk8-maven3

pipelines:
  branches:
    master:
      - step:
          name: Test
          script:
            - cd launcher && mvn package
            - cd app && mvn test
      - step:
          name: Deploy
          script:
            - cd app && mvn deploy -Dchannel=stable
          deployment: production
      